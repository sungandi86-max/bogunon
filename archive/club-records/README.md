# Club Records Archive

동아리 학생기록 UI는 학생 개인정보를 다루므로 BOGUNON v1.0 출시 대상에서 제외한다. 향후 BOGUNON Studio 또는 v2에서 개인정보 저장 방식과 권한 모델을 별도로 검토한 뒤 복원한다.

- 기존 UI 컴포넌트와 페이지 구현은 이 디렉터리에 보존한다.
- 현재 App Router와 내비게이션에서는 연결하지 않는다.
- Supabase 테이블과 migration은 변경하지 않는다.
